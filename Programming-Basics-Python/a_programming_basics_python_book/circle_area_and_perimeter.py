import math                                           # from math import pi
r = float(input("Enter circle radius => r = "))
area = math.pi * r * r                                # area = pi * r * r
perimeter = 2 * math.pi * r                           # perimeter = 2 * pi * r

print("Area = ", area)
print("Perimeter = ", perimeter)